# Vercel Free Deploy

1. Push this repo to GitHub.
2. Open Vercel.
3. Import the GitHub repo.
4. Deploy with default settings.

Notes:

- This app works as a static deploy.
- If `/api/state` is unavailable, it falls back to browser local storage.
- Data stays in the current browser unless you export and import a backup.
